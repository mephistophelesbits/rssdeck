# i18n EN/ZH-CN Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add English / Simplified Chinese language switching to RSS Deck with a single toggle button, auto-switching AI summary language and briefing report language.

**Architecture:** Lightweight custom `useTranslation()` hook backed by two JSON translation files. Locale stored in Zustand settings store and persisted via existing `/api/settings` mechanism. No external i18n library.

**Tech Stack:** React, Next.js App Router, Zustand, TypeScript

**Spec:** `docs/superpowers/specs/2026-03-19-i18n-en-zh-cn-design.md`

---

## Chunk 1: Foundation — Translation System & Settings Store

### Task 1: Create English translation file

**Files:**
- Create: `lib/i18n/en.json`

- [ ] **Step 1: Create the English translations JSON file**

```json
{
  "nav": {
    "rss": "RSS",
    "intelligence": "Intelligence",
    "briefings": "Briefings",
    "bookmarks": "Bookmarks",
    "search": "Search",
    "switchToLight": "Switch to Light Mode",
    "switchToDark": "Switch to Dark Mode",
    "switchLanguage": "Switch to Chinese"
  },
  "briefings": {
    "title": "Briefings",
    "description": "Manual-first daily briefs and follow-up chat grounded in the persisted article corpus.",
    "briefAutomation": "Brief Automation",
    "generating": "Generating…",
    "generateBriefing": "Generate Briefing",
    "savedBriefings": "Saved Briefings",
    "noBriefingsYet": "No briefings yet. Generate one to create a saved morning desk.",
    "noBriefingsEmpty": "No saved briefings yet. Generate the first manual briefing from the stored article corpus.",
    "rankedStories": "ranked stories",
    "deleteBriefing": "Delete briefing",
    "executiveBrief": "Executive Brief",
    "generatedFrom": "Generated {date} from the stored article corpus.",
    "stories": "Stories",
    "themes": "Themes",
    "sources": "Sources",
    "leadStory": "Lead Story",
    "noLeadStory": "No lead story selected",
    "themeStack": "Theme Stack",
    "briefingNotes": "Briefing Notes",
    "pushToTelegram": "Push to Telegram",
    "pushing": "Pushing…",
    "configureTelegram": "Configure Telegram in Brief Automation first",
    "pushThisBriefing": "Push this briefing to Telegram",
    "chatWithBriefing": "Chat With Briefing",
    "chatEmptyState": "Ask what changed, which stories matter most, or how the themes connect.",
    "chatPlaceholder": "Ask about the briefing, themes, or top stories",
    "sending": "Sending…",
    "send": "Send",
    "suggestedPrompts": "Suggested Prompts",
    "prompt1": "What are the biggest risks in this briefing?",
    "prompt2": "Which two stories are likely to evolve next?",
    "prompt3": "Summarize the briefing for a 60-second update.",
    "prompt4": "Show how the top themes connect across categories.",
    "whatChanged": "What Changed",
    "topSources": "Top Sources",
    "rankedStoryBoard": "Ranked Story Board",
    "storiesCount": "stories",
    "inCategory": "in",
    "lead": "Lead",
    "automationTitle": "Brief Automation",
    "automationDescription": "Choose when to auto-generate the daily brief and optionally push the current saved briefing to Telegram.",
    "close": "Close",
    "generateAutomatically": "Generate Automatically",
    "scheduleTimes": "Schedule Times",
    "addTime": "Add Time",
    "removeTime": "Remove scheduled time",
    "autoGenHelp": "Automatic generation runs when the app is open and the current time passes one of these slots.",
    "botToken": "Bot Token",
    "botTokenPlaceholder": "123456:ABC-DEF...",
    "chatId": "Chat ID",
    "chatIdPlaceholder": "e.g. 123456789",
    "telegramHelp": "The pushed message uses the current saved briefing summary and top stories.",
    "pushCurrentBriefing": "Push Current Briefing",
    "briefingPushed": "Briefing pushed to Telegram.",
    "hideTelegramToken": "Hide Telegram token",
    "showTelegramToken": "Show Telegram token",
    "toggleAutoGeneration": "Toggle automatic briefing generation",
    "toggleTelegram": "Toggle Telegram push"
  },
  "settings": {
    "title": "Settings",
    "general": "General",
    "aiAssistant": "AI Assistant",
    "colorTheme": "Color Theme",
    "autoRefresh": "Auto Refresh Interval",
    "min": "min",
    "defaultViewMode": "Default View Mode",
    "comfortable": "Comfortable",
    "compact": "Compact",
    "aiProvider": "AI Provider",
    "ollamaLocal": "Ollama (Local)",
    "openaiApi": "OpenAI (API)",
    "anthropicApi": "Anthropic (API)",
    "geminiApi": "Google Gemini (API)",
    "minimaxApi": "Minimax (API)",
    "kimiApi": "Kimi / Moonshot (API)",
    "apiKey": "API Key",
    "enterApiKey": "Enter {provider} API key",
    "hideApiKey": "Hide API key",
    "showApiKey": "Show API key",
    "ollamaUrl": "Ollama URL",
    "checkingConnection": "Checking connection...",
    "ollamaConnected": "Ollama connected",
    "ollamaNotRunning": "Ollama not running",
    "model": "Model",
    "modelPlaceholderOllama": "llama3.2",
    "modelPlaceholderCloud": "e.g. gpt-4o-mini",
    "ollamaInstallHint": "Run <code>ollama pull llama3.2</code> to install.",
    "summaryLanguage": "Summary Language",
    "langOriginal": "Original Language (Auto)",
    "langEnglish": "English",
    "langSpanish": "Spanish",
    "langFrench": "French",
    "langGerman": "German",
    "langChinese": "Chinese",
    "langJapanese": "Japanese",
    "langKorean": "Korean",
    "langPortuguese": "Portuguese",
    "customPrompt": "Custom Summary Prompt",
    "customPromptPlaceholder": "e.g. You are an expert analyst. Summarize this article in 3 bullet points.",
    "customPromptHelp": "Overrides the default AI instruction. Use <code>{{title}}</code> and <code>{{content}}</code> as placeholders.",
    "save": "Save",
    "saved": "Saved!",
    "testAi": "Test AI",
    "testing": "Testing...",
    "working": "Working!",
    "failed": "Failed",
    "connectionSuccessful": "Connection successful!",
    "requestTimedOut": "Request timed out (15s)",
    "settingsSavedAuto": "Settings are saved automatically"
  },
  "search": {
    "title": "Search",
    "subtitle": "Saved article search rules",
    "ruleNamePlaceholder": "Rule name (optional)",
    "saving": "Saving…",
    "saveSearch": "Save Search",
    "savedRules": "Saved Rules",
    "savedRulesEmpty": "Save a search to reuse your keyword rules later.",
    "ran": "Ran {date}",
    "notRunYet": "Not run yet",
    "articleSearch": "Article Search",
    "articleSearchDesc": "Search your stored corpus with comma-separated keywords. Example:",
    "articleSearchExample": "china, semiconductors, export controls",
    "results": "results",
    "keywordsPlaceholder": "Keywords separated by commas",
    "searching": "Searching…",
    "searchButton": "Search",
    "sourcesCount": "sources",
    "emptyTitle": "Search your stored articles",
    "emptyDesc": "Results include title, article snippet, source title, extracted themes, and entities from the local intelligence database.",
    "score": "score",
    "openOriginal": "Open original",
    "enterKeywords": "Enter one or more keywords separated by commas.",
    "runBeforeSaving": "Run or enter a search before saving it.",
    "searchRuleSaved": "Search rule saved locally.",
    "refreshResults": "Refresh search results"
  },
  "addFeed": {
    "title": "Add Feed",
    "addTo": "Add to",
    "newColumn": "New Column",
    "categories": "Categories",
    "customUrl": "Custom URL",
    "opml": "OPML",
    "feedsCount": "feeds",
    "rssFeedUrl": "RSS Feed URL",
    "urlPlaceholder": "https://example.com/feed.xml",
    "validating": "Validating...",
    "addAsNewColumn": "Add as New Column",
    "addToColumn": "Add to Column",
    "savedFeeds": "Saved Feeds",
    "exportOpml": "Export OPML",
    "deleteSavedFeed": "Delete saved feed",
    "enterUrl": "Enter the URL of any RSS or Atom feed",
    "dropOpml": "Drop your OPML file here",
    "orClickBrowse": "or click to browse",
    "selectFile": "Select File",
    "invalidOpml": "Invalid OPML file. Please check the format.",
    "noFeedsInOpml": "No RSS feeds found in this OPML file.",
    "failedParseOpml": "Failed to parse OPML file.",
    "foundFeeds": "Found {count} feeds",
    "clear": "Clear",
    "andMore": "...and {count} more",
    "importAsColumn": "Import as Column",
    "importAsMultipleColumns": "Import as Multiple Columns",
    "importHelp": "Choose to import all feeds into one column or create separate columns by category",
    "pleaseEnterUrl": "Please enter a URL"
  },
  "addColumn": {
    "title": "Add Column",
    "categories": "Categories",
    "customUrl": "Custom URL",
    "opmlImport": "OPML Import",
    "feedsCount": "feeds",
    "rssFeedUrl": "RSS Feed URL",
    "urlPlaceholder": "https://example.com/feed.xml",
    "validating": "Validating...",
    "addFeed": "Add Feed",
    "enterUrl": "Enter the URL of any RSS or Atom feed",
    "dropOpml": "Drop your OPML file here",
    "orClickBrowse": "or click to browse",
    "selectFile": "Select File",
    "invalidOpml": "Invalid OPML file. Please check the format.",
    "noFeedsInOpml": "No RSS feeds found in this OPML file.",
    "failedParseOpml": "Failed to parse OPML file.",
    "foundFeeds": "Found {count} feeds",
    "clear": "Clear",
    "andMore": "...and {count} more",
    "addAsOneColumn": "Add as One Column",
    "splitByCategory": "Split by Category",
    "importHelp": "Import feeds from Feedly, Inoreader, Google Reader, etc.",
    "pleaseEnterUrl": "Please enter a URL"
  },
  "article": {
    "aiSummaryDisabled": "AI Summary is disabled. Enable it in Settings.",
    "aiSummary": "AI Summary",
    "generatingSummary": "Generating summary...",
    "removeBookmark": "Remove bookmark",
    "addBookmark": "Add bookmark",
    "generateAiSummary": "Generate AI Summary"
  },
  "intelligence": {
    "title": "Intelligence",
    "subtitle": "NLP pipeline overview of the stored article corpus.",
    "articles": "Articles",
    "sources": "Sources",
    "mapPoints": "Map Points",
    "keywordCloud": "Keyword Cloud",
    "loadingOverview": "Loading overview…",
    "worldMap": "World Map",
    "keywordCategory": "Keyword Category",
    "category": "Category",
    "storiesLabel": "Stories",
    "keywordScore": "Keyword Score",
    "articlesLabel": "Articles",
    "mentions": "Mentions",
    "sourcesLabel": "Sources",
    "country": "Country",
    "selectCountry": "Select a country or city on the map to inspect coverage.",
    "trendMonitor": "Trend Monitor",
    "latestSnapshot": "Latest Snapshot",
    "storylines": "Storylines",
    "weight": "Weight",
    "noStorylines": "Not enough related coverage yet to form storylines.",
    "recentArticles": "Recent Stored Articles",
    "notEnoughHistory": "Not enough history yet."
  },
  "bookmarks": {
    "title": "Bookmarks",
    "savedCount": "({count} saved)",
    "noBookmarks": "No Bookmarks Yet",
    "noBookmarksDesc": "Click the bookmark icon on any article to save it",
    "openOriginal": "Open original",
    "removeBookmark": "Remove bookmark"
  },
  "landing": {
    "selfHosted": "SELF-HOSTED",
    "features": "Features",
    "privacy": "Privacy",
    "themesNav": "Themes",
    "selfHost": "Self-Host",
    "getStarted": "Get Started",
    "heroPrivateBadge": "100% Private & Self-Hosted",
    "heroTitle1": "Own Your Data.",
    "heroTitle2": "Command Your News.",
    "heroSubtitle": "A privacy-first RSS reader designed for power users. Run it on your <strong>NAS</strong>, <strong>Home Server</strong>, or <strong>Local PC</strong> with zero-cloud AI summarization powered by Ollama.",
    "livePreview": "Live Preview",
    "deployLocally": "Deploy Locally",
    "viewSource": "View Source",
    "builtForPowerUsers": "Built for Power Users",
    "networkSovereignty": "Network Sovereignty",
    "networkSovereigntyDesc": "Runs entirely on your LAN. Access via VPN or local IP. You own the hardware.",
    "localFirstAi": "Local-First AI",
    "localFirstAiDesc": "Leverages Ollama to run LLMs on your machine. Instant offline AI summaries.",
    "hostItAnywhere": "Host It Anywhere",
    "homeServer": "Home Server",
    "homeServerDesc": "Docker ready for ARM and x86.",
    "dockerHub": "Docker Hub",
    "selfHostCommand": "Self-Host Command",
    "github": "GitHub",
    "docker": "Docker",
    "copyright": "© 2026 RSS Deck. Open Source."
  },
  "common": {
    "delete": "Delete",
    "cancel": "Cancel",
    "confirm": "Confirm",
    "loading": "Loading...",
    "error": "Error",
    "success": "Success"
  }
}
```

- [ ] **Step 2: Verify the file was created**

Run: `cat lib/i18n/en.json | python3 -c "import sys,json; json.load(sys.stdin); print('Valid JSON')"`
Expected: `Valid JSON`

- [ ] **Step 3: Commit**

```bash
git add lib/i18n/en.json
git commit -m "feat(i18n): add English translation file"
```

---

### Task 2: Create Simplified Chinese translation file

**Files:**
- Create: `lib/i18n/zh-CN.json`

- [ ] **Step 1: Create the Chinese translations JSON file**

The structure mirrors `en.json` exactly. Every key present in `en.json` must be present here.

```json
{
  "nav": {
    "rss": "RSS",
    "intelligence": "情报",
    "briefings": "简报",
    "bookmarks": "收藏",
    "search": "搜索",
    "switchToLight": "切换为浅色模式",
    "switchToDark": "切换为深色模式",
    "switchLanguage": "切换为英文"
  },
  "briefings": {
    "title": "简报",
    "description": "基于已存储的文章语料库，手动生成每日简报并进行后续对话。",
    "briefAutomation": "自动化设置",
    "generating": "生成中…",
    "generateBriefing": "生成简报",
    "savedBriefings": "已保存的简报",
    "noBriefingsYet": "暂无简报。生成一份以创建您的每日简报台。",
    "noBriefingsEmpty": "暂无已保存的简报。从已存储的文章语料库中生成第一份手动简报。",
    "rankedStories": "篇已排序报道",
    "deleteBriefing": "删除简报",
    "executiveBrief": "执行摘要",
    "generatedFrom": "生成于 {date}，基于已存储的文章语料库。",
    "stories": "报道",
    "themes": "主题",
    "sources": "来源",
    "leadStory": "头条报道",
    "noLeadStory": "未选择头条报道",
    "themeStack": "主题堆叠",
    "briefingNotes": "简报笔记",
    "pushToTelegram": "推送至 Telegram",
    "pushing": "推送中…",
    "configureTelegram": "请先在自动化设置中配置 Telegram",
    "pushThisBriefing": "将此简报推送至 Telegram",
    "chatWithBriefing": "与简报对话",
    "chatEmptyState": "询问发生了什么变化、哪些报道最重要，或主题之间如何关联。",
    "chatPlaceholder": "询问关于简报、主题或热门报道的问题",
    "sending": "发送中…",
    "send": "发送",
    "suggestedPrompts": "建议提示",
    "prompt1": "这份简报中最大的风险是什么？",
    "prompt2": "哪两个报道最有可能继续发展？",
    "prompt3": "用60秒的内容总结这份简报。",
    "prompt4": "展示各类别中热门主题之间的关联。",
    "whatChanged": "变化摘要",
    "topSources": "热门来源",
    "rankedStoryBoard": "排序报道看板",
    "storiesCount": "篇报道",
    "inCategory": "于",
    "lead": "头条",
    "automationTitle": "自动化设置",
    "automationDescription": "选择何时自动生成每日简报，并可选择将当前已保存的简报推送至 Telegram。",
    "close": "关闭",
    "generateAutomatically": "自动生成",
    "scheduleTimes": "计划时间",
    "addTime": "添加时间",
    "removeTime": "移除计划时间",
    "autoGenHelp": "当应用打开且当前时间经过设定时段时，将自动生成简报。",
    "botToken": "Bot Token",
    "botTokenPlaceholder": "123456:ABC-DEF...",
    "chatId": "Chat ID",
    "chatIdPlaceholder": "例如 123456789",
    "telegramHelp": "推送消息使用当前已保存的简报摘要和热门报道。",
    "pushCurrentBriefing": "推送当前简报",
    "briefingPushed": "简报已推送至 Telegram。",
    "hideTelegramToken": "隐藏 Telegram Token",
    "showTelegramToken": "显示 Telegram Token",
    "toggleAutoGeneration": "切换自动生成简报",
    "toggleTelegram": "切换 Telegram 推送"
  },
  "settings": {
    "title": "设置",
    "general": "通用",
    "aiAssistant": "AI 助手",
    "colorTheme": "颜色主题",
    "autoRefresh": "自动刷新间隔",
    "min": "分钟",
    "defaultViewMode": "默认视图模式",
    "comfortable": "舒适",
    "compact": "紧凑",
    "aiProvider": "AI 供应商",
    "ollamaLocal": "Ollama（本地）",
    "openaiApi": "OpenAI（API）",
    "anthropicApi": "Anthropic（API）",
    "geminiApi": "Google Gemini（API）",
    "minimaxApi": "Minimax（API）",
    "kimiApi": "Kimi / Moonshot（API）",
    "apiKey": "API 密钥",
    "enterApiKey": "输入 {provider} API 密钥",
    "hideApiKey": "隐藏 API 密钥",
    "showApiKey": "显示 API 密钥",
    "ollamaUrl": "Ollama URL",
    "checkingConnection": "正在检查连接...",
    "ollamaConnected": "Ollama 已连接",
    "ollamaNotRunning": "Ollama 未运行",
    "model": "模型",
    "modelPlaceholderOllama": "llama3.2",
    "modelPlaceholderCloud": "例如 gpt-4o-mini",
    "ollamaInstallHint": "运行 <code>ollama pull llama3.2</code> 进行安装。",
    "summaryLanguage": "摘要语言",
    "langOriginal": "原始语言（自动）",
    "langEnglish": "English",
    "langSpanish": "Español",
    "langFrench": "Français",
    "langGerman": "Deutsch",
    "langChinese": "中文",
    "langJapanese": "日本語",
    "langKorean": "한국어",
    "langPortuguese": "Português",
    "customPrompt": "自定义摘要提示词",
    "customPromptPlaceholder": "例如：你是一位专家分析师。用3个要点总结这篇文章。",
    "customPromptHelp": "覆盖默认的 AI 指令。使用 <code>{{title}}</code> 和 <code>{{content}}</code> 作为占位符。",
    "save": "保存",
    "saved": "已保存！",
    "testAi": "测试 AI",
    "testing": "测试中...",
    "working": "正常工作！",
    "failed": "失败",
    "connectionSuccessful": "连接成功！",
    "requestTimedOut": "请求超时（15秒）",
    "settingsSavedAuto": "设置自动保存"
  },
  "search": {
    "title": "搜索",
    "subtitle": "已保存的文章搜索规则",
    "ruleNamePlaceholder": "规则名称（可选）",
    "saving": "保存中…",
    "saveSearch": "保存搜索",
    "savedRules": "已保存的规则",
    "savedRulesEmpty": "保存搜索以便日后重用关键词规则。",
    "ran": "运行于 {date}",
    "notRunYet": "尚未运行",
    "articleSearch": "文章搜索",
    "articleSearchDesc": "使用逗号分隔的关键词搜索已存储的语料库。示例：",
    "articleSearchExample": "中国, 半导体, 出口管制",
    "results": "结果",
    "keywordsPlaceholder": "以逗号分隔的关键词",
    "searching": "搜索中…",
    "searchButton": "搜索",
    "sourcesCount": "个来源",
    "emptyTitle": "搜索已存储的文章",
    "emptyDesc": "搜索结果包括标题、文章摘要、来源标题、提取的主题和来自本地情报数据库的实体。",
    "score": "评分",
    "openOriginal": "打开原文",
    "enterKeywords": "请输入一个或多个以逗号分隔的关键词。",
    "runBeforeSaving": "请先运行或输入搜索再保存。",
    "searchRuleSaved": "搜索规则已本地保存。",
    "refreshResults": "刷新搜索结果"
  },
  "addFeed": {
    "title": "添加订阅源",
    "addTo": "添加至",
    "newColumn": "新建列",
    "categories": "分类",
    "customUrl": "自定义 URL",
    "opml": "OPML",
    "feedsCount": "个订阅源",
    "rssFeedUrl": "RSS 订阅源 URL",
    "urlPlaceholder": "https://example.com/feed.xml",
    "validating": "验证中...",
    "addAsNewColumn": "添加为新列",
    "addToColumn": "添加到列",
    "savedFeeds": "已保存的订阅源",
    "exportOpml": "导出 OPML",
    "deleteSavedFeed": "删除已保存的订阅源",
    "enterUrl": "输入任何 RSS 或 Atom 订阅源的 URL",
    "dropOpml": "将 OPML 文件拖放到此处",
    "orClickBrowse": "或点击浏览",
    "selectFile": "选择文件",
    "invalidOpml": "无效的 OPML 文件。请检查格式。",
    "noFeedsInOpml": "此 OPML 文件中未找到 RSS 订阅源。",
    "failedParseOpml": "解析 OPML 文件失败。",
    "foundFeeds": "找到 {count} 个订阅源",
    "clear": "清除",
    "andMore": "...以及其他 {count} 个",
    "importAsColumn": "导入为一列",
    "importAsMultipleColumns": "导入为多列",
    "importHelp": "选择将所有订阅源导入一列或按分类创建多列",
    "pleaseEnterUrl": "请输入 URL"
  },
  "addColumn": {
    "title": "添加列",
    "categories": "分类",
    "customUrl": "自定义 URL",
    "opmlImport": "OPML 导入",
    "feedsCount": "个订阅源",
    "rssFeedUrl": "RSS 订阅源 URL",
    "urlPlaceholder": "https://example.com/feed.xml",
    "validating": "验证中...",
    "addFeed": "添加订阅源",
    "enterUrl": "输入任何 RSS 或 Atom 订阅源的 URL",
    "dropOpml": "将 OPML 文件拖放到此处",
    "orClickBrowse": "或点击浏览",
    "selectFile": "选择文件",
    "invalidOpml": "无效的 OPML 文件。请检查格式。",
    "noFeedsInOpml": "此 OPML 文件中未找到 RSS 订阅源。",
    "failedParseOpml": "解析 OPML 文件失败。",
    "foundFeeds": "找到 {count} 个订阅源",
    "clear": "清除",
    "andMore": "...以及其他 {count} 个",
    "addAsOneColumn": "添加为一列",
    "splitByCategory": "按分类拆分",
    "importHelp": "从 Feedly、Inoreader、Google Reader 等导入订阅源。",
    "pleaseEnterUrl": "请输入 URL"
  },
  "article": {
    "aiSummaryDisabled": "AI 摘要已禁用。请在设置中启用。",
    "aiSummary": "AI 摘要",
    "generatingSummary": "正在生成摘要...",
    "removeBookmark": "移除收藏",
    "addBookmark": "添加收藏",
    "generateAiSummary": "生成 AI 摘要"
  },
  "intelligence": {
    "title": "情报",
    "subtitle": "已存储文章语料库的 NLP 管线概览。",
    "articles": "文章",
    "sources": "来源",
    "mapPoints": "地图标记",
    "keywordCloud": "关键词云",
    "loadingOverview": "正在加载概览…",
    "worldMap": "世界地图",
    "keywordCategory": "关键词分类",
    "category": "分类",
    "storiesLabel": "报道",
    "keywordScore": "关键词评分",
    "articlesLabel": "文章",
    "mentions": "提及次数",
    "sourcesLabel": "来源",
    "country": "国家",
    "selectCountry": "在地图上选择一个国家或城市以查看覆盖情况。",
    "trendMonitor": "趋势监控",
    "latestSnapshot": "最新快照",
    "storylines": "故事线",
    "weight": "权重",
    "noStorylines": "尚无足够的相关报道来形成故事线。",
    "recentArticles": "近期存储的文章",
    "notEnoughHistory": "历史数据不足。"
  },
  "bookmarks": {
    "title": "收藏",
    "savedCount": "（{count} 条已保存）",
    "noBookmarks": "暂无收藏",
    "noBookmarksDesc": "点击任何文章上的收藏图标即可保存",
    "openOriginal": "打开原文",
    "removeBookmark": "移除收藏"
  },
  "landing": {
    "selfHosted": "自托管",
    "features": "功能",
    "privacy": "隐私",
    "themesNav": "主题",
    "selfHost": "自托管",
    "getStarted": "开始使用",
    "heroPrivateBadge": "100% 私有化 & 自托管",
    "heroTitle1": "数据自主。",
    "heroTitle2": "掌控新闻。",
    "heroSubtitle": "一款为高级用户设计的隐私优先 RSS 阅读器。在您的 <strong>NAS</strong>、<strong>家庭服务器</strong>或<strong>本地电脑</strong>上运行，配合 Ollama 实现零云端 AI 摘要。",
    "livePreview": "在线预览",
    "deployLocally": "本地部署",
    "viewSource": "查看源码",
    "builtForPowerUsers": "为高级用户打造",
    "networkSovereignty": "网络主权",
    "networkSovereigntyDesc": "完全运行在您的局域网上。通过 VPN 或本地 IP 访问。您拥有硬件。",
    "localFirstAi": "本地优先 AI",
    "localFirstAiDesc": "利用 Ollama 在您的设备上运行大语言模型。即时离线 AI 摘要。",
    "hostItAnywhere": "随处部署",
    "homeServer": "家庭服务器",
    "homeServerDesc": "支持 ARM 和 x86 的 Docker 部署。",
    "dockerHub": "Docker Hub",
    "selfHostCommand": "自托管命令",
    "github": "GitHub",
    "docker": "Docker",
    "copyright": "© 2026 RSS Deck。开源项目。"
  },
  "common": {
    "delete": "删除",
    "cancel": "取消",
    "confirm": "确认",
    "loading": "加载中...",
    "error": "错误",
    "success": "成功"
  }
}
```

- [ ] **Step 2: Verify the file was created**

Run: `cat lib/i18n/zh-CN.json | python3 -c "import sys,json; json.load(sys.stdin); print('Valid JSON')"`
Expected: `Valid JSON`

- [ ] **Step 3: Commit**

```bash
git add lib/i18n/zh-CN.json
git commit -m "feat(i18n): add Simplified Chinese translation file"
```

---

### Task 3: Create the useTranslation hook

**Files:**
- Create: `lib/i18n/index.ts`

- [ ] **Step 1: Write the translation hook**

```typescript
import { useSettingsStore } from '@/lib/settings-store';
import en from './en.json';
import zhCN from './zh-CN.json';

export type Locale = 'en' | 'zh-CN';

const translations: Record<Locale, Record<string, unknown>> = {
  'en': en,
  'zh-CN': zhCN,
};

function getNestedValue(obj: Record<string, unknown>, path: string): string {
  const keys = path.split('.');
  let current: unknown = obj;
  for (const key of keys) {
    if (current === null || current === undefined || typeof current !== 'object') {
      return path;
    }
    current = (current as Record<string, unknown>)[key];
  }
  return typeof current === 'string' ? current : path;
}

export function useTranslation() {
  const locale = useSettingsStore((state) => state.locale);

  const t = (key: string, params?: Record<string, string | number>): string => {
    let value = getNestedValue(translations[locale], key);

    // Fall back to English if key not found in current locale
    if (value === key && locale !== 'en') {
      value = getNestedValue(translations['en'], key);
    }

    // Replace template parameters like {date}, {count}, {provider}
    if (params) {
      for (const [paramKey, paramValue] of Object.entries(params)) {
        value = value.replace(`{${paramKey}}`, String(paramValue));
      }
    }

    return value;
  };

  return { t, locale };
}
```

- [ ] **Step 2: Verify TypeScript compiles**

Run: `npx tsc --noEmit lib/i18n/index.ts 2>&1 | head -20`
Expected: No errors (or only unrelated existing errors)

- [ ] **Step 3: Commit**

```bash
git add lib/i18n/index.ts
git commit -m "feat(i18n): add useTranslation hook with fallback to English"
```

---

### Task 4: Add locale to settings store

**Files:**
- Modify: `lib/settings-store.ts`

- [ ] **Step 1: Add locale to SettingsState interface**

In `lib/settings-store.ts`, add `locale` and `setLocale` to the `SettingsState` interface (after the `articleAgeFilter` line):

```typescript
// After line 58 (articleAgeFilter):
  locale: 'en' | 'zh-CN';
  setLocale: (locale: 'en' | 'zh-CN') => void;
```

- [ ] **Step 2: Add locale to SettingsSnapshot**

Add `'locale'` to the `SettingsSnapshot` Pick type:

```typescript
export type SettingsSnapshot = Pick<
  SettingsState,
  | 'themeId'
  | 'defaultRefreshInterval'
  | 'defaultViewMode'
  | 'showPreviewPanel'
  | 'articleAgeFilter'
  | 'locale'
  | 'aiSettings'
  | 'briefingSettings'
>;
```

- [ ] **Step 3: Add locale to getDefaultSettingsSnapshot**

Add `locale: 'en'` to the return value of `getDefaultSettingsSnapshot()`:

```typescript
export function getDefaultSettingsSnapshot(): SettingsSnapshot {
  return {
    themeId: 'stitch-dark',
    defaultRefreshInterval: 10,
    defaultViewMode: 'comfortable',
    showPreviewPanel: true,
    articleAgeFilter: 'all',
    locale: 'en',
    aiSettings: { ... },
    briefingSettings: { ... },
  };
}
```

- [ ] **Step 4: Add locale to toSettingsSnapshot**

Add `locale: state.locale` to the `toSettingsSnapshot` function:

```typescript
function toSettingsSnapshot(state: SettingsState): SettingsSnapshot {
  return {
    themeId: state.themeId,
    defaultRefreshInterval: state.defaultRefreshInterval,
    defaultViewMode: state.defaultViewMode,
    showPreviewPanel: state.showPreviewPanel,
    articleAgeFilter: state.articleAgeFilter,
    locale: state.locale,
    aiSettings: state.aiSettings,
    briefingSettings: state.briefingSettings,
  };
}
```

- [ ] **Step 5: Add setLocale action to the store**

In the `create<SettingsState>()` call, add the `setLocale` action:

```typescript
setLocale: (locale) =>
  set((state) => {
    const language = locale === 'zh-CN' ? 'Chinese' : 'Original Language';
    const aiSettings = { ...state.aiSettings, language };
    const nextState = { ...state, locale, aiSettings };
    persistSettings(toSettingsSnapshot(nextState));
    return { locale, aiSettings };
  }),
```

- [ ] **Step 6: Fix hydrateSettings to merge with defaults**

Change `hydrateSettings` from:
```typescript
hydrateSettings: (settings) => set(() => settings),
```
to:
```typescript
hydrateSettings: (settings) => set(() => ({
  ...getDefaultSettingsSnapshot(),
  ...settings,
})),
```

This ensures existing users without `locale` in their persisted settings get the default `'en'`.

- [ ] **Step 7: Verify TypeScript compiles**

Run: `npx tsc --noEmit 2>&1 | head -20`
Expected: No new errors

- [ ] **Step 8: Commit**

```bash
git add lib/settings-store.ts
git commit -m "feat(i18n): add locale state and setLocale action to settings store"
```

---

## Chunk 2: UI Components — TopNavBar & Settings

### Task 5: Add language toggle to TopNavBar

**Files:**
- Modify: `components/ui/TopNavBar.tsx`

- [ ] **Step 1: Import useTranslation and update NAV_ITEMS**

Replace the import section and NAV_ITEMS constant. Change the `label` field to `labelKey`:

```typescript
import { useTranslation } from '@/lib/i18n';

const NAV_ITEMS = [
  { href: '/', labelKey: 'nav.rss', icon: Rss },
  { href: '/intelligence', labelKey: 'nav.intelligence', icon: BrainCircuit },
  { href: '/briefings', labelKey: 'nav.briefings', icon: Newspaper },
  { href: '/bookmarks', labelKey: 'nav.bookmarks', icon: Bookmark },
];
```

- [ ] **Step 2: Use t() in the component**

Inside the `TopNavBar` component function, add:

```typescript
const { t, locale } = useTranslation();
const setLocale = useSettingsStore((state) => state.setLocale);
```

Update the nav items render to use `t(item.labelKey)` instead of `item.label`:

```tsx
title={t(item.labelKey)}
// ...
<span className={cn("text-[10px] uppercase font-semibold tracking-wider", active ? "text-accent" : "hidden md:block")}>
  {t(item.labelKey)}
</span>
```

- [ ] **Step 3: Add the language toggle button**

Between the Search link and the theme toggle button (after line 138, before line 140), add:

```tsx
<button
  onClick={() => setLocale(locale === 'en' ? 'zh-CN' : 'en')}
  title={t('nav.switchLanguage')}
  className="w-9 h-9 flex items-center justify-center rounded-full text-foreground-secondary hover:bg-background-tertiary hover:text-foreground transition-colors font-semibold text-xs"
>
  {locale === 'en' ? 'EN' : '中'}
</button>
```

- [ ] **Step 4: Translate the theme toggle title**

Change the theme toggle title from:
```tsx
title={isDarkMode ? "Switch to Light Mode" : "Switch to Dark Mode"}
```
to:
```tsx
title={isDarkMode ? t('nav.switchToLight') : t('nav.switchToDark')}
```

- [ ] **Step 5: Translate the Search link title**

Change:
```tsx
title="Search"
```
to:
```tsx
title={t('nav.search')}
```

- [ ] **Step 6: Verify the app compiles**

Run: `npm run build 2>&1 | tail -20`
Expected: Build succeeds

- [ ] **Step 7: Commit**

```bash
git add components/ui/TopNavBar.tsx
git commit -m "feat(i18n): add language toggle button to TopNavBar and translate nav labels"
```

---

### Task 6: Translate SettingsModal

**Files:**
- Modify: `components/ui/SettingsModal.tsx`

- [ ] **Step 1: Import useTranslation**

Add at the top of the imports:
```typescript
import { useTranslation } from '@/lib/i18n';
```

Inside the component function, add:
```typescript
const { t } = useTranslation();
```

- [ ] **Step 2: Replace all hardcoded strings with t() calls**

Replace all hardcoded English strings. Key replacements:

| Line | Original | Replacement |
|------|----------|-------------|
| 139 | `"Settings"` | `{t('settings.title')}` |
| 155 | `"General"` | `{t('settings.general')}` |
| 165 | `"AI Assistant"` | `{t('settings.aiAssistant')}` |
| 179 | `"Color Theme"` | `{t('settings.colorTheme')}` |
| 213 | `"Auto Refresh Interval"` | `{t('settings.autoRefresh')}` |
| 227 | `` `${mins} min` `` | `` `${mins} ${t('settings.min')}` `` |
| 237 | `"Default View Mode"` | `{t('settings.defaultViewMode')}` |
| 249 | `"Comfortable"` | `{t('settings.comfortable')}` |
| 260 | `"Compact"` | `{t('settings.compact')}` |
| 274 | `"AI Assistant"` | `{t('settings.aiAssistant')}` |
| 297 | `"AI Provider"` | `{t('settings.aiProvider')}` |
| 312-317 | Provider option labels | `{t('settings.ollamaLocal')}`, etc. |
| 325 | `"API Key"` | `{t('settings.apiKey')}` |
| 335 | `` `Enter ${aiSettings.provider} API key` `` | `{t('settings.enterApiKey', { provider: aiSettings.provider })}` |
| 354 | `"Ollama URL"` | `{t('settings.ollamaUrl')}` |
| 368 | `"Checking connection..."` | `{t('settings.checkingConnection')}` |
| 374 | `"Ollama connected"` | `{t('settings.ollamaConnected')}` |
| 380 | `"Ollama not running"` | `{t('settings.ollamaNotRunning')}` |
| 390 | `"Model"` | `{t('settings.model')}` |
| 423 | `"Summary Language"` | `{t('settings.summaryLanguage')}` |
| 430-439 | Language option labels | `{t('settings.langOriginal')}`, etc. |
| 446 | `"Custom Summary Prompt"` | `{t('settings.customPrompt')}` |
| 470-472 | `"Saved!" / "Save"` | `{t('settings.saved')} / {t('settings.save')}` |
| 486-492 | Test AI button states | `{t('settings.testing')}`, etc. |
| 513 | `"Settings are saved automatically"` | `{t('settings.settingsSavedAuto')}` |

- [ ] **Step 3: Hide the Summary Language dropdown**

The Summary Language dropdown (lines 420-440) should be hidden because locale controls it. Wrap the section in a condition:

```tsx
{/* Summary Language is controlled by locale toggle — hide it */}
```

Remove or comment out the Summary Language `<div>` block entirely (lines 420-440).

- [ ] **Step 4: Verify the app compiles**

Run: `npm run build 2>&1 | tail -20`
Expected: Build succeeds

- [ ] **Step 5: Commit**

```bash
git add components/ui/SettingsModal.tsx
git commit -m "feat(i18n): translate SettingsModal and hide language dropdown"
```

---

## Chunk 3: Core Feature Components

### Task 7: Translate BriefingsWorkspace

**Files:**
- Modify: `components/BriefingsWorkspace.tsx`

- [ ] **Step 1: Import useTranslation**

```typescript
import { useTranslation } from '@/lib/i18n';
```

Inside the component:
```typescript
const { t, locale } = useTranslation();
```

- [ ] **Step 2: Replace all hardcoded strings with t() calls**

This is the largest component. Replace every hardcoded English string with the corresponding `t()` call. Key replacements (by line range):

**Header section (lines 277-284):**
- `"Briefings"` → `{t('briefings.title')}`
- `"Manual-first daily briefs..."` → `{t('briefings.description')}`
- `"Brief Automation"` → `{t('briefings.briefAutomation')}`
- `"Generating…" / "Generate Briefing"` → `{t('briefings.generating')} / {t('briefings.generateBriefing')}`

**Saved briefings sidebar (lines 296-350):**
- `"Saved Briefings"` → `{t('briefings.savedBriefings')}`
- `"No briefings yet..."` → `{t('briefings.noBriefingsYet')}`
- `"ranked stories"` → `{t('briefings.rankedStories')}`
- `"Delete briefing"` → `{t('briefings.deleteBriefing')}`

**Executive Brief section (lines 356-520):**
- `"Executive Brief"` → `{t('briefings.executiveBrief')}`
- `"Generated {date}..."` → `{t('briefings.generatedFrom', { date: new Date(selectedBriefing.briefingDate).toLocaleString() })}`
- `"Stories" / "Themes" / "Sources"` → `{t('briefings.stories')} / {t('briefings.themes')} / {t('briefings.sources')}`
- `"Lead Story" / "Theme Stack"` → `{t('briefings.leadStory')} / {t('briefings.themeStack')}`
- `"Briefing Notes"` → `{t('briefings.briefingNotes')}`
- `"Push to Telegram"` → `{t('briefings.pushToTelegram')}`
- `"Chat With Briefing"` → `{t('briefings.chatWithBriefing')}`
- All suggested prompts → `{t('briefings.prompt1')}`, etc.
- `"What Changed" / "Top Sources"` → `{t('briefings.whatChanged')} / {t('briefings.topSources')}`

**Ranked Story Board (lines 519-561):**
- `"Ranked Story Board"` → `{t('briefings.rankedStoryBoard')}`
- `"stories"` → `{t('briefings.storiesCount')}`
- `"Lead"` → `{t('briefings.lead')}`
- `"#{index + 1} in {category}"` → `` `#${index + 1} ${t('briefings.inCategory')} ${category}` ``

**Automation modal (lines 575-723):**
- All labels similarly translated

**Empty state (lines 563-568):**
- `"No saved briefings yet..."` → `{t('briefings.noBriefingsEmpty')}`

- [ ] **Step 3: Pass locale in generateBriefing request**

In `handleGenerate`, add `locale` to the request body:

```typescript
body: JSON.stringify({ aiSettings, locale }),
```

- [ ] **Step 4: Pass locale in pushToTelegram request**

In `handlePushToTelegram`, add `locale` to the request body:

```typescript
body: JSON.stringify({
  briefing: selectedBriefing,
  telegramToken: briefingSettings.telegramToken,
  telegramChatId: briefingSettings.telegramChatId,
  locale,
}),
```

- [ ] **Step 5: Translate workspace messages**

Change line 170:
```typescript
setWorkspaceMessage('Briefing pushed to Telegram.');
```
to:
```typescript
setWorkspaceMessage(t('briefings.briefingPushed'));
```

- [ ] **Step 6: Verify the app compiles**

Run: `npm run build 2>&1 | tail -20`
Expected: Build succeeds

- [ ] **Step 7: Commit**

```bash
git add components/BriefingsWorkspace.tsx
git commit -m "feat(i18n): translate BriefingsWorkspace and pass locale to API"
```

---

### Task 8: Translate SearchWorkspace

**Files:**
- Modify: `components/SearchWorkspace.tsx`

- [ ] **Step 1: Import and use useTranslation**

```typescript
import { useTranslation } from '@/lib/i18n';
// Inside component:
const { t } = useTranslation();
```

- [ ] **Step 2: Replace all hardcoded strings**

Key replacements:
- `"Search"` → `{t('search.title')}`
- `"Saved article search rules"` → `{t('search.subtitle')}`
- `"Rule name (optional)"` → `t('search.ruleNamePlaceholder')`
- `"Saving…" / "Save Search"` → `{t('search.saving')} / {t('search.saveSearch')}`
- `"Saved Rules"` → `{t('search.savedRules')}`
- `"Save a search..."` → `{t('search.savedRulesEmpty')}`
- `"Article Search"` → `{t('search.articleSearch')}`
- `"Search your stored corpus..."` → `{t('search.articleSearchDesc')}`
- `"Keywords separated by commas"` → `t('search.keywordsPlaceholder')`
- `"Searching…" / "Search"` → `{t('search.searching')} / {t('search.searchButton')}`
- `"sources"` → `{t('search.sourcesCount')}`
- `"Search your stored articles"` → `{t('search.emptyTitle')}`
- `"Results include title..."` → `{t('search.emptyDesc')}`
- `"score"` → `{t('search.score')}`
- `"results"` → `{t('search.results')}`
- All workspace messages use `t()` equivalents
- `"Enter one or more keywords..."` → `t('search.enterKeywords')`
- `"Run or enter a search..."` → `t('search.runBeforeSaving')`
- `"Search rule saved locally."` → `t('search.searchRuleSaved')`

- [ ] **Step 3: Commit**

```bash
git add components/SearchWorkspace.tsx
git commit -m "feat(i18n): translate SearchWorkspace"
```

---

### Task 9: Translate ArticleCard

**Files:**
- Modify: `components/deck/ArticleCard.tsx`

- [ ] **Step 1: Import and use useTranslation**

```typescript
import { useTranslation } from '@/lib/i18n';
// Inside component:
const { t } = useTranslation();
```

- [ ] **Step 2: Replace hardcoded strings**

- `alert('AI Summary is disabled...')` → `alert(t('article.aiSummaryDisabled'))`
- `"AI Summary"` → `{t('article.aiSummary')}`
- `"Generating summary..."` → `{t('article.generatingSummary')}`
- `"Remove bookmark" / "Add bookmark"` → `{t('article.removeBookmark')} / {t('article.addBookmark')}`
- `"Generate AI Summary"` → `{t('article.generateAiSummary')}`

- [ ] **Step 3: Commit**

```bash
git add components/deck/ArticleCard.tsx
git commit -m "feat(i18n): translate ArticleCard"
```

---

### Task 10: Translate BookmarksModal

**Files:**
- Modify: `components/ui/BookmarksModal.tsx`

- [ ] **Step 1: Import and use useTranslation**

```typescript
import { useTranslation } from '@/lib/i18n';
// Inside component:
const { t } = useTranslation();
```

- [ ] **Step 2: Replace hardcoded strings**

- `"Bookmarks"` → `{t('bookmarks.title')}`
- `"({bookmarks.length} saved)"` → `{t('bookmarks.savedCount', { count: bookmarks.length })}`
- `"No Bookmarks Yet"` → `{t('bookmarks.noBookmarks')}`
- `"Click the bookmark icon..."` → `{t('bookmarks.noBookmarksDesc')}`
- `"Open original"` → `{t('bookmarks.openOriginal')}`
- `"Remove bookmark"` → `{t('bookmarks.removeBookmark')}`

- [ ] **Step 3: Commit**

```bash
git add components/ui/BookmarksModal.tsx
git commit -m "feat(i18n): translate BookmarksModal"
```

---

### Task 11: Translate AddFeedModal

**Files:**
- Modify: `components/ui/AddFeedModal.tsx`

- [ ] **Step 1: Import and use useTranslation**

```typescript
import { useTranslation } from '@/lib/i18n';
// Inside component:
const { t } = useTranslation();
```

- [ ] **Step 2: Replace all hardcoded strings**

Replace every hardcoded string using the `addFeed.*` namespace keys. Key replacements include:
- `"Add Feed"` → `{t('addFeed.title')}`
- `"Add to"` → `{t('addFeed.addTo')}`
- `"New Column"` → `{t('addFeed.newColumn')}`
- `"Categories" / "Custom URL" / "OPML"` → `{t('addFeed.categories')} / {t('addFeed.customUrl')} / {t('addFeed.opml')}`
- `"feeds"` → `{t('addFeed.feedsCount')}`
- `"RSS Feed URL"` → `{t('addFeed.rssFeedUrl')}`
- Error messages, buttons, help text — all use corresponding `t()` keys
- `"Found {count} feeds"` → `{t('addFeed.foundFeeds', { count: opmlFeeds.length })}`
- `"...and {count} more"` → `{t('addFeed.andMore', { count: opmlFeeds.length - 10 })}`
- `"Please enter a URL"` → `t('addFeed.pleaseEnterUrl')`

- [ ] **Step 3: Commit**

```bash
git add components/ui/AddFeedModal.tsx
git commit -m "feat(i18n): translate AddFeedModal"
```

---

### Task 12: Translate AddColumnModal

**Files:**
- Modify: `components/ui/AddColumnModal.tsx`

- [ ] **Step 1: Import and use useTranslation**

```typescript
import { useTranslation } from '@/lib/i18n';
// Inside component:
const { t } = useTranslation();
```

- [ ] **Step 2: Replace all hardcoded strings**

Replace every hardcoded string using the `addColumn.*` namespace keys. Same pattern as AddFeedModal.

- [ ] **Step 3: Commit**

```bash
git add components/ui/AddColumnModal.tsx
git commit -m "feat(i18n): translate AddColumnModal"
```

---

## Chunk 4: Intelligence, Landing Page & API Changes

### Task 13: Translate IntelligenceDashboard

**Files:**
- Modify: `components/IntelligenceDashboard.tsx`

- [ ] **Step 1: Import and use useTranslation**

```typescript
import { useTranslation } from '@/lib/i18n';
// Inside component:
const { t } = useTranslation();
```

- [ ] **Step 2: Replace all hardcoded strings**

Key replacements:
- `"Intelligence"` → `{t('intelligence.title')}`
- `"NLP pipeline overview..."` → `{t('intelligence.subtitle')}`
- `"Articles" / "Sources" / "Map Points"` → MetricCard labels use `t()`
- `"Keyword Cloud" / "World Map" / "Trend Monitor"` → section headings
- `"Category" / "Stories" / "Keyword Score"` → detail panel labels
- `"Storylines"` → `{t('intelligence.storylines')}`
- `"Weight"` → `{t('intelligence.weight')}`
- All empty states and help text

- [ ] **Step 3: Commit**

```bash
git add components/IntelligenceDashboard.tsx
git commit -m "feat(i18n): translate IntelligenceDashboard"
```

---

### Task 14: Translate LandingPage

**Files:**
- Modify: `components/LandingPage.tsx`

- [ ] **Step 1: Import and use useTranslation**

```typescript
import { useTranslation } from '@/lib/i18n';
// Inside component:
const { t } = useTranslation();
```

- [ ] **Step 2: Replace all hardcoded strings**

The landing page has extensive marketing copy. Replace all text using the `landing.*` namespace keys:

- Nav links: `"Features"`, `"Privacy"`, `"Themes"`, `"Self-Host"` → `t()` equivalents
- Hero badge: `"100% Private & Self-Hosted"` → `{t('landing.heroPrivateBadge')}`
- Hero titles: `"Own Your Data."` / `"Command Your News."` → `{t('landing.heroTitle1')}` / `{t('landing.heroTitle2')}`
- Hero subtitle uses `dangerouslySetInnerHTML={{ __html: t('landing.heroSubtitle') }}` (since it contains `<strong>` tags)
- Buttons: `"Live Preview"` / `"Deploy Locally"` / `"View Source"`
- Feature titles and descriptions
- Deploy section text
- Footer text

- [ ] **Step 3: Also translate the carousel labels array**

Change:
```typescript
const labels = ['Cyberpunk Theme', 'Warm Amber Theme', 'Matrix Theme'];
```
These are theme names, which are technical. Keep them as-is per the spec (not translated).

- [ ] **Step 4: Commit**

```bash
git add components/LandingPage.tsx
git commit -m "feat(i18n): translate LandingPage"
```

---

### Task 15: Add Chinese briefing generation to API

**Files:**
- Modify: `app/api/briefings/generate/route.ts`

- [ ] **Step 1: Read locale from request body**

After line 23, add:
```typescript
const locale = body.locale || 'en';
```

- [ ] **Step 2: Modify the prompt for Chinese locale**

After the prompt string is constructed (after line 141), add:

```typescript
const localizedPrompt = locale === 'zh-CN'
  ? prompt + '\n\nIMPORTANT: Write the entire response in Simplified Chinese (简体中文), including all section headings. Use these exact headings: ## 执行摘要, ## 关键主题, ## 重要性分析'
  : prompt;
```

- [ ] **Step 3: Use localizedPrompt in generateText call**

Change line 145 from:
```typescript
prompt,
```
to:
```typescript
localizedPrompt,
```

- [ ] **Step 4: Localize the briefing title**

Change line 155 from:
```typescript
title: `Daily Briefing ${new Date().toLocaleDateString()}`,
```
to:
```typescript
title: locale === 'zh-CN'
  ? `每日简报 ${new Date().toLocaleDateString()}`
  : `Daily Briefing ${new Date().toLocaleDateString()}`,
```

- [ ] **Step 5: Commit**

```bash
git add app/api/briefings/generate/route.ts
git commit -m "feat(i18n): generate briefing in Chinese when locale is zh-CN"
```

---

### Task 16: Localize Telegram push labels

**Files:**
- Modify: `app/api/briefings/push/route.ts`

- [ ] **Step 1: Read locale from request body**

After line 46, add:
```typescript
const locale = body.locale || 'en';
```

- [ ] **Step 2: Localize the section labels in formatBriefingMessage**

Update `formatBriefingMessage` to accept a `locale` parameter:

```typescript
function formatBriefingMessage(briefing: { ... }, locale: string) {
```

Change the label strings:
```typescript
const keyThemesLabel = locale === 'zh-CN' ? '关键主题' : 'Key Themes';
const topStoriesLabel = locale === 'zh-CN' ? '热门报道' : 'Top Stories';

return [
  `🗞️ <b>${escapeHtml(briefing.title)}</b>`,
  '',
  safeSummary,
  themes ? `\n<b>${keyThemesLabel}</b>\n${themes}` : '',
  topStories ? `\n<b>${topStoriesLabel}</b>\n${topStories}` : '',
].filter(Boolean).join('\n');
```

- [ ] **Step 3: Pass locale to formatBriefingMessage**

Change line 57 from:
```typescript
text: formatBriefingMessage(briefing),
```
to:
```typescript
text: formatBriefingMessage(briefing, locale),
```

- [ ] **Step 4: Commit**

```bash
git add app/api/briefings/push/route.ts
git commit -m "feat(i18n): localize Telegram push message labels"
```

---

### Task 17: Update html lang attribute

**Files:**
- Modify: `app/layout.tsx`
- Create: `components/HtmlLangUpdater.tsx`

- [ ] **Step 1: Create a client component to update html lang**

Since `app/layout.tsx` is a server component, we need a client component to dynamically update the `lang` attribute:

Create `components/HtmlLangUpdater.tsx`:

```typescript
'use client';

import { useEffect } from 'react';
import { useSettingsStore } from '@/lib/settings-store';

export function HtmlLangUpdater() {
  const locale = useSettingsStore((state) => state.locale);

  useEffect(() => {
    document.documentElement.lang = locale;
  }, [locale]);

  return null;
}
```

- [ ] **Step 2: Add HtmlLangUpdater to AppProviders or layout**

In `app/layout.tsx`, add the `HtmlLangUpdater` inside the body:

```tsx
<AppProviders>
  <HtmlLangUpdater />
  {children}
</AppProviders>
```

Import at the top:
```typescript
import { HtmlLangUpdater } from '@/components/HtmlLangUpdater';
```

- [ ] **Step 3: Commit**

```bash
git add components/HtmlLangUpdater.tsx app/layout.tsx
git commit -m "feat(i18n): dynamically update html lang attribute based on locale"
```

---

## Chunk 5: Final Verification

### Task 18: Full build and verification

- [ ] **Step 1: Run full TypeScript check**

Run: `npx tsc --noEmit 2>&1 | tail -30`
Expected: No new errors

- [ ] **Step 2: Run full build**

Run: `npm run build 2>&1 | tail -30`
Expected: Build succeeds

- [ ] **Step 3: Run lint**

Run: `npm run lint 2>&1 | tail -30`
Expected: No new warnings/errors

- [ ] **Step 4: Verify JSON files have matching keys**

Run a quick script to check both translation files have the same keys:
```bash
node -e "
const en = require('./lib/i18n/en.json');
const zh = require('./lib/i18n/zh-CN.json');
function getKeys(obj, prefix='') {
  return Object.entries(obj).flatMap(([k,v]) =>
    typeof v === 'object' ? getKeys(v, prefix+k+'.') : [prefix+k]
  );
}
const enKeys = new Set(getKeys(en));
const zhKeys = new Set(getKeys(zh));
const missingInZh = [...enKeys].filter(k => !zhKeys.has(k));
const extraInZh = [...zhKeys].filter(k => !enKeys.has(k));
if (missingInZh.length) console.log('Missing in zh-CN:', missingInZh);
if (extraInZh.length) console.log('Extra in zh-CN:', extraInZh);
if (!missingInZh.length && !extraInZh.length) console.log('All keys match!');
"
```
Expected: `All keys match!`

- [ ] **Step 5: Final commit if any fixups needed**

```bash
git add -A
git commit -m "fix(i18n): address build/lint issues"
```
